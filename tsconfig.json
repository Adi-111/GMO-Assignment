{
  "compilerOptions": {
    "target": "ES6",                         // Specifies ECMAScript target version.
    "module": "ESNext",                      // Specifies module code generation.
    "jsx": "react-jsx",                      // Enables JSX syntax for React.
    "strict": true,                          // Enable all strict type-checking options.
    "moduleResolution": "node",              // Determines how modules are resolved.
    "esModuleInterop": true,                 // Enables interoperability between CommonJS and ES Modules.
    "skipLibCheck": true,                    // Skip type checking of declaration files.
    "forceConsistentCasingInFileNames": true // Ensure consistent file naming.
  },
  "include": ["src"],                        // Specify the files to include in the project.
  "exclude": ["node_modules", "dist"]        // Exclude these directories from the project.
}